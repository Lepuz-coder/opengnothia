// Expo's ambient types (`declare module "*.css"` among them). Normally these
// live in the generated, git-ignored expo-env.d.ts; keeping our own committed
// copy means `tsc` works on a fresh clone without running the Expo CLI first.
/// <reference types="expo/types" />
